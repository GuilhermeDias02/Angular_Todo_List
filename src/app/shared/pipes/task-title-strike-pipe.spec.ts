import { TaskTitleStrikePipe } from './task-title-strike-pipe';

describe('TaskTitleStrikePipe', () => {
  it('create an instance', () => {
    const pipe = new TaskTitleStrikePipe();
    expect(pipe).toBeTruthy();
  });
});
